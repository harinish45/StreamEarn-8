name: Projects Safety Build

on:
  push:
    branches: [projects-jwt-fix]
    paths:
      - 'src/lib/project-store.ts'
      - '.github/workflows/projects-safety.yml'
      - 'package.json'
      - 'package-lock.json'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --fund=false
      - run: npm run typecheck
      - run: npm run build

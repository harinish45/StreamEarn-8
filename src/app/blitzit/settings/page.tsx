
import { Settings } from "@/app/blitzit/components/Settings";

// This is a placeholder for the dedicated settings page.
export default function SettingsPage() {
    return (
        <div>
            <h1 className="text-3xl font-bold mb-6">Settings</h1>
            {/* The sheet component's content will be rendered here directly for the page view */}
        </div>
    )
}
